# test.R
x <- rnorm(100)
# Crée un vecteur de 100 nombres aléatoires suivant une distribution normale standard

# Calcule la moyenne
mean_x <- mean(x)

# Affiche dans le terminal
print(paste("La moyenne des x est vfjkcdvd:", round(mean_x, 2)))
